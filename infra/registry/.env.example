# Registry Service Environment
PORT=8100

# Postgres (same instance as Directus)
DATABASE_URL=postgres://directus:PASSWORD@localhost:5432/directus

# Redis
REDIS_URL=redis://:PASSWORD@localhost:6379/4

# Session config
SESSION_TTL_SECONDS=1800
SESSION_TOKEN_BYTES=32
